# Optimalysy — спек MVP (результат grill-сессии)

Сервис подбора оптимального набора лабораторий под заказанную корзину анализов:
покрыть всё нужное + дешевле + подсветить «бонусные» анализы, достающиеся почти даром.
Ментальная модель — **Aviasales для анализов**.

## 1. Амбиция и монетизация
- **Сейчас:** личный инструмент (частая личная потребность).
- **Потом:** возможный CPA-агрегатор в духе **sravni.ru**. У лаб есть партнёрки → CPA-интеграция
  обычно приносит и прайс-фид/API, т.е. будущий заработок и будущий источник данных — одна дверь.
- Модель данных с первого дня закладываем так, чтобы пережить переход «один юзер → витрина».

## 2. Секвенирование
- **MVP:** прогнать реальный заказ врача (~12 аналитов) по 6 лабам Москвы. Данные — собираются
  под конкретную корзину, мэтчинг руками. Артефакт — локальный Python-скрипт.
- **Фаза 1:** on-demand парсеры по лабам (HTML-first, brightdata при анти-боте).
- **Фаза 2:** полный каталог крови (~500 позиций/лаба × 6) + ежедневный парсер + фаззи-мэтчер
  с человеческой валидацией. Под CPA-будущее.

## 3. Целевая функция («что такое оптимально»)
```
минимизировать: Σ(цены выбранных позиций/комплексов) + сбор_за_забор × (число задействованных лаб)
```
- **Забор биоматериала учитывается** — один раз за визит на лабу (Гемотест публикует, напр. 330 ₽).
  Именно он делает разбивку по лабам часто невыгодной.
- **Комплексы** — альтернативные «наборы» в задаче покрытия; часто дешевле суммы штучных.
- Два режима результата (Aviasales):
  - **«Самый дешёвый»** = глобальный минимум, разрешена разбивка по лабам (= рейс с пересадками,
    каждая доп. лаба = свой сбор за забор).
  - **«Самый дешёвый в одной лабе»** = лучший single-lab (= прямой рейс, один визит).
  - Показываем рядом с **дельтой** между ними.
- Алгоритм: ≤6 лаб × ≤12 аналитов + комплексы → пространство крошечное, брутфорс/ILP мгновенны.
  Оптимизатор — **не** узкое место.

## 4. Фича «бонусные анализы за те же деньги»
Относительно **базовой линии** = самый дешёвый способ покрыть РОВНО заказ.
- **(α) Комплекс дешевле штучного набора и содержит лишнее** → оптимизатор выбирает его сам,
  лишнее «бесплатно в довесок». Это не фича, это честная работа оптимизатора.
- **(β) Комплекс чуть дороже базы, но добирает ценное** → отдельный блок «Апгрейды за копейки»,
  никогда не выбирается автоматически, только саджест.

**Правило показа β:**
- кандидаты — только комплексы, релевантные заказу (содержат ≥1 заказанный аналит);
- **только в пределах лабы с самой дешёвой корзиной** (никаких новых визитов ради бонуса);
- показываем если: переплата над базой ≤ **10%** ИЛИ штучная цена любого бонусного анализа ≥ **2×** переплаты;
- гранулярность: **(P)** замена корзины целиком одним комплексом — в MVP; **(Q)** частичная замена
  под-части плана — расширение сразу после;
- лишнее помечаем «бонус», ценность — по штучной цене, без «тебе это нужно».

## 5. Мэтчинг (самая тяжёлая часть)
- **Общего кодификатора у лаб НЕТ.** «Код на бланке» (Гемотест `3.4`, `1.110.1`) — внутренний код лабы,
  не склеивает аналиты между лабами. LOINC не публикуется.
- Ключ — **рукотворный канонический справочник**: аналит ← (название + синонимы + биоматериал + единицы)
  каждой лабы. Новую позицию всегда подтверждает человек.
- Гемотест на странице анализа сам перечисляет **входящие комплексы** (обратная ссылка аналит → комплексы) —
  сильно упрощает сбор релевантных комплексов on-demand.

## 6. Разрешение неоднозначности ввода (8A → ii)
Слова врача ≠ SKU лабы. Модель: **клинический дефолт + видимый переключатель**.
- Т3 → **свободный**, Т4 → **свободный**, тестостерон → **общий** (стандарт РФ).
- В нашем прогоне используем дефолты, но с явной плашкой **«⚠️ уточните: бывают варианты»**.

## 7. Непокрытие / feasibility (8B → i+ii)
Некоторые аналиты (B6, цинк, ДГТ) есть не в каждой лабе.
- Полный single-lab показываем, если существует лаба, покрывающая всё.
- Если нет — лучший **частичный** single-lab + явно «чего не хватает и где добрать» + глобальный микс рядом.
  (Aviasales: «прямых нет, вот с 1 пересадкой».)
- Обязательное поле: для каждого аналит × лаба — **факт наличия** (present/absent), не только цена.

## 8. Модель данных (семя Фазы 2)
```
canonical_analyte: { id, name, fraction (free/total/—), synonyms[] }
lab_offering:      { analyte_id, lab, present: bool, price, code_на_бланке }
complex:           { lab, name, price, contains: [analyte_id...] }
biomaterial_fee:   { lab, price }
```
Сейчас — JSON/CSV; та же структура ложится в БД потом.

## 9. MVP — форма и данные
- Форма: **локальный Python-скрипт** (вход — канонический список; выход — таблица: single-lab / микс /
  блок бонусов + дельты).
- Данные первого прогона: **собираются под заказ** (WebFetch/браузер; brightdata при анти-боте).

## 10. Конкретные вводные прогона
- **Город:** Москва.
- **Лаборатории (6):** Гемотест, Инвитро, KDL, Хеликс, CMD, LabQuest.
- **Заказ врача → 12 аналитов:**
  1. 25-OH витамин D
  2. B6 (пиридоксин) — редкий, есть не везде
  3. B9 (фолиевая кислота)
  4. B12
  5. железо сывороточное (Fe)
  6. ферритин
  7. цинк (Zn) — редкий
  8. Т3 свободный ⚠️
  9. Т4 свободный ⚠️
  10. ТТГ
  11. тестостерон общий ⚠️
  12. ДГТ (дигидротестостерон) — редкий, часто на аутсорс

## Открытые вопросы (осознанно отложены)
- CPA-интеграции и переговоры с лабами — Фаза 2.
- Фаззи-мэтчер полного каталога + пайплайн валидации — Фаза 2.
- UI/веб, мультигород, персистентность — после проверки гипотезы.
